export const criticalCSS = `
  /* Inserisci qui il CSS critico */
  :root {
    --background: 0 0% 100%;
    --foreground: 222.2 84% 4.9%;
    /* Aggiungi altre variabili CSS critiche */
  }

  body {
    background-color: hsl(var(--background));
    color: hsl(var(--foreground));
  }

  /* Aggiungi altri stili critici necessari per il rendering iniziale */
`

